export type {
  CardTypePip,
  CardTypePipSymbol,
  GenreTheme,
} from "@repo/cards-domain";

/** Shipped deck: how catalogue № is bucketed (genre series vs. country/region series). */
export type { CatalogSeriesType } from "@repo/cards-domain";
